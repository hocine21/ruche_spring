version: '3.8'

services:
  app:
    image: openjdk:17-jdk
    container_name: ruche_spring_app
    working_dir: /app
    volumes:
      - ./:/app
    command: ["./mvnw", "spring-boot:run"]
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev